import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexBetweenLeft = styled(Box)({
    display:"flex",
    justifyContent: "space-around",
    alignItems:"left"
})

export default FlexBetweenLeft;